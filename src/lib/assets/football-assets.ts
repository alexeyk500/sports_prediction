export function getTeamAssetUrl(slug: string): string {
  return `/assets/teams/${slug}.webp`;
}

export function getCompetitionAssetUrl(slug: string): string {
  return `/assets/competitions/${slug}.webp`;
}
