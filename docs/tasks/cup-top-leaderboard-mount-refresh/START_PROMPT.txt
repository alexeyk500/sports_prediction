Реализуй задачу из docs/tasks/cup-top-leaderboard-mount-refresh/CODEX_TASK.md.

Удали `entryVersion` и всю связанную навигационную логику. Initial Top 50 должен обновляться просто при каждом mount/open `CupScreen`.

Store guard оставь только против concurrent загрузки: `idle/loaded/error -> loading` разрешены, `loading -> false`. Поэтому новый реальный вход в Cup после завершённой предыдущей загрузки делает fresh `GET ...leaderboard?mode=top&limit=50`, а Strict Mode synthetic remount во время уже идущей загрузки не отправляет второй concurrent request.

Не возвращай Top в bootstrap. Не используй hasLoadedRef/useRef hacks, debounce, timers, generic in-flight Map или новый entry token/version.

CupLeaderboard сам не должен загружать Top; Around Me / All / pagination оставь lazy и без изменений.

После реализации проверь dev Strict Mode Network: первый вход = 1 Top request; Cup -> Matches -> Cup = ещё 1 fresh request. Выполни lint, typecheck, unit tests и build.
